sudo apt update

# Neovim
sudo apt install neovim

# Git
sudo add-apt-repository ppa:git-core/ppa
sudo apt update
sudo apt install git
git config --global --user.email "hatimmohammed369@gmail.com"
git config --global --user.name  "Hatim Muhammed"
git config --global credential.helper store
git config --global core.editor nvim
git config --global color.ui true
git config --global sendemail.from = hatimmohammed369@gmail.com
git config --global sendemail.smtpServer = smtp.gmail.com
git config --global sendemail.smtpServerPort = 587
git config --global sendemail.smtpEncryption = tls
git config --global sendemail.smtpUser = hatimmohammed369@gmail.com
git config --global sendemail.smtppass = 2357911Gts_VIRTON
git config --global sendemail.suppresscc = self
git config --global sendemail.chainreplyto = false
# Curl
sudo apt install curl

# Oh-My-Zsh
sh -c "$(curl -fsSL https://raw.githubusercontent.com/ohmyzsh/ohmyzsh/master/tools/install.sh)"

# Powerline fonts:
sudo apt install fonts-powerline

# Powerlevel10k ZSH Theme
git clone --depth=1 https://github.com/romkatv/powerlevel10k.git ~/powerlevel10k
echo 'source ~/powerlevel10k/powerlevel10k.zsh-theme' >>~/.zshrc
p10k configure
